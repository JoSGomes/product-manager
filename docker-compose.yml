services:
  db:
    image: postgres
    volumes:
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
    environment:
      POSTGRES_PASSWORD: "123qwe"
      POSTGRES_DB: "products_api"
    ports:
      - "5432:5432"